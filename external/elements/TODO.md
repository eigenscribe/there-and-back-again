# Stylesheet Changes

- [ ] green tags $\rightarrow$ font too large
- [ ] Blue Gradients 💙🩵
  - [ ] In the `source` directory, replace all `var(--gradient-lite-to-dark-blue)` with `var(--gradient-blue)`
  - [ ] Remove all `var(--gradient-lite-to-dark-blue)` from the `assets/elements/..` directory.
  
    > 🎗️ Make sure to remove it from `theme-variables.css` once you are certain there is no mention of it in the other `elements` files.


- [ ] Add theme for `bash` codeblocks.
- [ ] Change the 'PreTeXt' codeblock theme (decide whether to use an upgraded version of the old one or some original code block styling).
- 
# UI Testing 

> - [ ] Grid-like component (similar `grid` and `grid cards` classes in zensical)
>
>    - [ ] `sidebyside` element:
>      
>        ```xml
>        <sidebyside widths="33% 33% 33%">
>            <figure>...</figure>
>            <figure>...</figure>
>            <figure>...</figure>
>        </sidebyside>
>        ```
>    - [ ] `tabular` element:
>
>        ```xml
>        <tabular>
>          ...
>        </tabular>
>        ```

